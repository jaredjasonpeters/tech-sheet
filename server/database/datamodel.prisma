type User {
  id: ID! @unique
  email: String!@unique
  password: String!
  name: String!
  companies: [Company!]
  techsheets: [Techsheet!]!
}

type Techsheet {
  id: ID! @unique
  title: String!
  published: Boolean! @default(value: "false")
  author: User
}

enum Company {
  DLFPICKSEED
  SEEDRESEARCHOFOREGON
}